import type { ThemeConfig } from "antd";

/**
 * @ref: https://ant.design/theme-editor#Style
 * ? Update theme config with help of above them editor by antd
 */
const theme: ThemeConfig = {
  token: {
    colorPrimary: "#ea495c",
    colorInfo: "#ea495c",
    borderRadius: 4,
  },
};

export default theme;
