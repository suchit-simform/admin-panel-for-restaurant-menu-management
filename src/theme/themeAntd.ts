import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    // Seed Token
    colorPrimary: "#040b6b",
    borderRadius: 4,

    // Alias Token
    colorBgContainer: "#f6ffed",
  },
};

export default theme;
